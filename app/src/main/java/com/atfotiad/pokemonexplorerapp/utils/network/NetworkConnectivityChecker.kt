package com.atfotiad.pokemonexplorerapp.utils.network
/**
 * [NetworkConnectivityChecker] is an interface that checks if internet is available]
 * */
interface NetworkConnectivityChecker {
    fun isInternetAvailable(): Boolean
}